import os
import base64
import requests
import datetime
from requests.auth import HTTPBasicAuth

def generate_access_token():
    """Generates access token"""

    #SandBox app keys
    # consumer_key = "Cp8hZIOFImG242bfmDlhxtiYE6phuew6"
    # consumer_secret = "jOo0CLfr3rdMD5GM"
    #api_URL = "https://sandbox.safaricom.co.ke/oauth/v1/generate?grant_type=client_credentials"

    #Production app keys
    consumer_key = "azKmbMO93xcGBQzaTACEVxrQ5a8ziG4C"
    consumer_secret = "Ajpcs6v6QuRIUXAO"
    api_URL = "https://api.safaricom.co.ke/oauth/v1/generate?grant_type=client_credentials"

    try:
        r = requests.get(api_URL, auth=HTTPBasicAuth(consumer_key, consumer_secret))
    except:
        r = requests.get(api_URL, auth=HTTPBasicAuth(consumer_key, consumer_secret), verify=False)

    json_response = (r.json())
    # json_response = r
    # import pdb; pdb.set_trace()
    my_access_token = json_response["access_token"]
    return my_access_token